import 'package:flutter/material.dart';

class MusteriPage extends StatelessWidget {
  const MusteriPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepPurple.shade300,
    );
  }
}